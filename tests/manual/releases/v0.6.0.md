# v0.6.0 supervised release checklist

Host SHA: `6f75a64c461070b07af1f89ad97fcf2473694eb8`

Mod SHA: `c0965b8e97dbde997a72044272edbf0dcdafa66a`

Tester/date: Codex supervised LOVE2D run, 2026-08-10

## Rocket Credit vertical slice

- [x] Red and Blue: loan shark is reachable and dialogue fits.
- [x] Rank offer, fee, total, and due badge are understandable before YES.
- [x] A second loan is refused while debt remains.
- [x] Coin repayment and money repayment update the same statement.
- [x] The due badge enters DEFAULT and adds exactly one fixed late fee.
- [x] Reopening menus never duplicates a fee.
- [x] Full repayment returns the account to CLEAR.
- [x] Gamble Mode off creates no debt and exposes no usable credit service.

## Last-resort family-home quest

- [x] Red and Blue: `LAST RESORT` appears only at exactly zero money and coins
  with the Coin Case present.
- [x] The confirmation clearly states 10,000 coins, loss of the family home,
  the 30,000-coin buyback, and the required battle.
- [x] Acceptance grants exactly 10,000 once and never changes a separate debt.
- [x] The occupied house has two readable Rocket tenants, authentic imported
  Rocket Hideout/Silph Co equipment, and no blocked door, stair, PC, route, or
  story path.
- [x] Mom appears upstairs, her new dialogue fits, and she still heals HP,
  status, and PP.
- [x] The deed costs exactly 30,000 once; declining or lacking funds changes
  nothing.
- [x] Losing the Rocket battle preserves the deed and permits a retry after
  save/reload.
- [x] Winning restores the original home and Mom exactly once without clearing
  unrelated debt/default state.
- [x] Every critical row in `tests/manual/V0.6_IN_GAME_TESTING.md` passes on
  both Red and Blue.

Evidence directories: `/tmp/blackjack-corner-v06/red` and
`/tmp/blackjack-corner-v06/blue`

Findings:

- Fixed a release-blocking interaction bug: the custom house-challenger talk
  handler intercepted the engine trainer path, so the visible Rocket never
  started a battle. The challenger now uses the normal trainer flow; a real
  loss, reload, retry, and win passed on Red and Blue.
- Replaced unsupported `+` and `>` characters in Blackjack, Hold'em, Tube
  Flyer, and Horse Racing native-font labels. Replayed game rounds emit no
  missing-glyph warnings.
- Rewrote pawn FIFO and deed-buyback confirmations to fit the two-line native
  text box. The oldest pawn, 30,000-coin price, and required Rocket battle are
  now visible beside the YES/NO choice rather than scrolling offscreen.
- Validation passed against fixture and imported bases; ROM-derived-content
  lint passed; the package contains 71 release files plus its pack manifest
  and excludes tests, drivers, fixtures, ROMs, and saves.
- The host `modkit.py pack` command stamps `packed_at` with wall-clock time, so
  archives built in different seconds have different hashes even though the
  payload listing is normalized. This is a host-tool limitation, not mod
  payload drift.
