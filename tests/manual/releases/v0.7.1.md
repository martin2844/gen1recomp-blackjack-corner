# v0.7.1 supervised release checklist

Host SHA: `6f75a64c461070b07af1f89ad97fcf2473694eb8`

Reviewed implementation SHA: `727501b` plus release-only version and checklist
metadata.

Tester/date: native LÖVE2D drivers, screenshot review, and automated gates,
2026-08-12.

ROM SHA-1s: Red `ea9bcae617fdf159b045185467ae58b2e4a48b9a`; Blue
`d7037c83e1ae5b39bde3c30787637ba1d4c48ce2`. ROMs and generated evidence stay
outside the repository and release archive.

## Settings and new-game flow

- [x] Both ROMs render the eight-row bundled settings page plus Reset Defaults;
  the four-row viewport scrolls cleanly to every fallback shiny control.
- [x] Every setting survives a real process restart, and Reset Defaults restores
  the documented values.
- [x] A supported external shiny renderer leaves the four core settings plus
  Reset Defaults on both ROMs without activating the bundled renderer.
- [x] The Gamble Mode YES/NO prompt appears before Oak or his welcome text. Red
  verifies the configurable YES default; Blue verifies the NO default.
- [x] Confirmation remains explicit, accepted Gamble Mode still grants the Coin
  Case and 100 coins, and changing the global default never mutates the saved
  campaign choice.
- [x] Pokemon Randomizer v0.46.4 completes its real setup on both ROMs with
  Gamble Mode accepted and declined. Wild encounters and ordinary trainers
  remain randomized, while only accepted Gamble Mode owns Oak's roulette.

## Runtime controls

- [x] Table Intros changes a live Blackjack interaction without bypassing the
  Coin Case requirement.
- [x] Disabling ordinary table introductions never skips KINGPIN Arena copy.
- [x] Shiny Upgrades changes NORMAL / SHINY / CANCEL to NORMAL / CANCEL and
  back without affecting ordinary prize delivery.
- [x] Relaxed, Normal, and Fast provide ordered shared reveal clocks for the
  five deterministic presentations. Crash and Tube Flyer remain outside that
  clock so their difficulty is unchanged.

## Review, regression, and package

- [x] Three independent big-review passes cover runtime/caller behavior,
  persistence/provider composition, intent, tests, documentation, and release
  boundaries. No production-code finding survived active disproof.
- [x] The one release-process finding—an unsigned native settings matrix—was
  fixed with a repeatable driver and completed Red/Blue execution record.
- [x] All 2,121 Lua assertions pass on the pinned host.
- [x] Fixture validation, imported validation, ROM-content lint, diff hygiene,
  and archive integrity pass.
- [x] The release candidate contains 67 payload files plus
  `.modkit/pack.json`; tests, native drivers, screenshots, ROMs, saves, and
  identities are absent.

External evidence root: `/tmp/blackjack-settings-native`.

The focused case definitions and detailed execution record are retained in
[`../SETTINGS_IN_GAME_TESTING.md`](../SETTINGS_IN_GAME_TESTING.md).
